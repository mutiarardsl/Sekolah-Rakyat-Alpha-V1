/**
 * SR MVP — AdminView
 * Renders AdminContent which handles its own state, nav, and pages.
 */
import AdminContent from './AdminContent';

const AdminView = () => <AdminContent />;

export default AdminView;
